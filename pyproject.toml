[project]
name = "zuwerk"
version = "0.0.1"
description = "Projects for humans and agents"
requires-python = ">=3.11"
license = "LicenseRef-OSaasy"

[tool.ruff]
target-version = "py311"
line-length = 100
