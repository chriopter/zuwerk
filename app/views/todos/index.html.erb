<% content_for :title, "Todos · Zuwerk" %>
<section class="mx-auto max-w-[1500px]"><div class="grid items-start gap-6 lg:grid-cols-[250px_minmax(0,1fr)]"><%= render "navigation" %><div class="rounded-xl border border-dashed border-stone-300 bg-white p-10 text-center"><h1 class="text-xl font-semibold">No todos yet</h1><p class="mt-2 text-sm text-stone-500">Create the first item for <%= @project.name %>.</p><%= link_to "New todo", new_project_todo_path(@project), class: "btn mt-5 bg-stone-900 text-white" %></div></div></section>
